error handling: binary plists, plists that are unparsable, plists that are not readable
usage for cmd2launchd
refactor for cmd2launchd
more tests
might want to shell escape output commands so they are directly usable as commands
  http://svn.ruby-lang.org/repos/ruby/trunk/lib/shellwords.rb

